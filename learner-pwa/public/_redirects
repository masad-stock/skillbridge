# Netlify redirects file for React Router
# This ensures that all routes are handled by React Router

/*    /index.html   200

# Optional: Redirect old URLs or add custom redirects
# /old-path    /new-path    301